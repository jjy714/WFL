



function MakeDessertPresentable(dessertData){

    for(var key in dessertData){
        if (key === 'Food name'){
            dessertresult = dataAfterComputation[key]
        }
        else{
            continue
        }
    }

    return dessertresult;
}